1) Описание

Бот хранит рецепты и помогает быстро их находить: есть каталог с пагинацией, поиск по названию/ингредиентам, случайный рецепт, избранное, добавление рецептов с фото. У администратора есть право удалять рецепты с подтверждением.

3) Возможности

Пользователя
📚 Каталог: просмотр списка рецептов, переход по страницам, открытие рецепта нажатием на кнопку с названием.
🔎 Поиск: поиск по названию и ингредиентам (ввод текстом).
🍲 Случайный рецепт: выдаёт случайный рецепт из базы.

⭐ Избранное:
Добавление/удаление рецепта в избранное кнопками ⭐/❌ под рецептом.
Просмотр избранного списком с пагинацией.

➕ Добавление рецепта:
Название → ингредиенты → шаги → фото (можно пропустить).
Фото отправляется отдельным сообщением, текст рецепта — отдельным (чтобы не упираться в лимит подписи у фото).

Администратор
🗑 Удаление рецептов:
Кнопка «Удалить рецепт» видна только администратору.
Удаление выполняется с подтверждением (✅ Да / ❌ Отмена).
После удаления рецепт исчезает из каталога и удаляется из избранного у пользователей.

3) Структура данных
recipes.json
Файл с рецептами в формате JSON (лежит рядом со скриптом). Каждый рецепт — объект:
id (int) — уникальный идентификатор.
title (str) — название.
ingredients (list[str]) — список ингредиентов.
steps (str) — шаги приготовления.
photo_file_id (str|null) — идентификатор фото Telegram (если добавляли фото).
Пример записи:

json
{
  "id": 12,
  "title": "Омлет",
  "ingredients": ["Яйца (2 шт.)", "Молоко (50 мл)", "Соль"],
  "steps": "Взбей и обжарь.",
  "photo_file_id": "AgACAgIAAxkBAA..."
}
bot_data_persistence.pkl
Файл хранения данных пользователей (избранное). Создаётся автоматически рядом со скриптом.
В user_data хранится ключ favs — список id рецептов, добавленных в избранное.

4) Требования
Windows + Python 3.11.x (подходит).
Установленная библиотека python-telegram-bot.
Токен бота от @BotFather.

6) Установка и запуск (Windows + VS Code)
Открой терминал в VS Code в папке проекта.
Установи зависимости:
powershell
python -m pip install --upgrade pip
python -m pip install --upgrade python-telegram-bot
Укажи токен:
В коде: замени PASTE_YOUR_TOKEN_HERE на токен
или
Через переменную окружения (в текущем терминале):
powershell
$env:BOT_TOKEN="ТВОЙ_ТОКЕН"
python botCatalog.py

Запуск:
powershell
python botCatalog.py
6) Первичная настройка администратора
Запусти бота.
В Telegram напиши боту команду:
/myid — бот ответит твоим user_id.
Останови бота, вставь это число в переменную:
ADMIN_ID = ...
Запусти снова.

7) Команды и кнопки
Команды
/start — старт и главное меню.
/catalog — открыть каталог.
/random — случайный рецепт.
/favs — открыть избранное.
/myid — показать текущий user_id (для настройки админа).
Кнопки главного меню
📚 Каталог
🍲 Случайный рецепт
🔎 Поиск
➕ Добавить рецепт
⭐ Избранное
Inline-кнопки
В каталоге: названия рецептов + ⬅️➡️ пагинация.
В рецепте: ⭐/❌ избранное, (для админа) 🗑 удалить, быстрые переходы в каталог/избранное.

8) Типичные проблемы
InvalidToken: неверный токен, проверь вставку токена или переменную BOT_TOKEN.
No module named telegram: пакет не установлен в текущий Python; ставь через python -m pip ... в том же терминале, где запускаешь.
Фото “не работает”: обычно ошибка в photo_file_id (не сохранён/не тот). Проверь, что рецепт добавлялся с фото именно через бота, и photo_file_id записался в recipes.json.

9) Расширение функционала (идеи)
Категории/теги, фильтр по категориям в каталоге.
Рейтинг рецептов и “топ”.

Список покупок из нескольких рецептов.

Экспорт/импорт recipes.json через отправку файла в чат.
